<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cex.crypto.trading.mapper.UserBalanceMapper">

    <resultMap id="UserBalanceResultMap" type="cex.crypto.trading.domain.UserBalance">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="currency" column="currency"/>
        <result property="availableBalance" column="available_balance"/>
        <result property="frozenBalance" column="frozen_balance"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="cex.crypto.trading.domain.UserBalance"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_balances (user_id, currency, available_balance, frozen_balance, created_at, updated_at)
        VALUES (#{userId}, #{currency}, #{availableBalance}, #{frozenBalance}, NOW(6), NOW(6))
    </insert>

    <select id="findByUserIdAndCurrency" resultMap="UserBalanceResultMap">
        SELECT * FROM user_balances
        WHERE user_id = #{userId} AND currency = #{currency}
    </select>

    <select id="findByUserId" resultMap="UserBalanceResultMap">
        SELECT * FROM user_balances
        WHERE user_id = #{userId}
        ORDER BY currency
    </select>

    <update id="updateAvailableBalance">
        UPDATE user_balances
        SET available_balance = available_balance + #{amount},
            updated_at = NOW(6)
        WHERE user_id = #{userId} AND currency = #{currency}
    </update>

    <update id="updateFrozenBalance">
        UPDATE user_balances
        SET frozen_balance = frozen_balance + #{amount},
            updated_at = NOW(6)
        WHERE user_id = #{userId} AND currency = #{currency}
    </update>

    <delete id="deleteById">
        DELETE FROM user_balances WHERE id = #{id}
    </delete>

</mapper>
